Encapsulation is the practice of bundling data(fields) and methods(functions) that operate on the data into a single unit (class). It also involes restricting direct access to some components using acccess modifiers (e.g., private, protected).